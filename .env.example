# Frontend distribution defaults
# Public image hosted on GHCR
IMAGE_NAME=ghcr.io/atijaguar-ec/fe-frontend
TAG=latest

# Container settings
# HTTP_PORT maps host port to container 80 (nginx)
HTTP_PORT=80
